{
  "name": "examples",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "rm -rf node_modules && bun i && bun run ./bundler/bundler.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.1.6"
  },
  "peerDependencies": {
    "typescript": "^5.0.0",
    "@the-brains/github-db": "link:@the-brains/github-db"
  }
}
