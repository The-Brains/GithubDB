{
  "name": "examples",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "server": "npx local-web-server --directory .",
    "build": "rm -rf node_modules && bun i && bun build --target=node ./src/index.ts --outfile=dist/index.js"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0",
    "githubdb": "link:githubdb"
  }
}
